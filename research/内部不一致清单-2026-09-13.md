# 代码内部不一致清单（2026-09-13 只读审计）

来源：对 `app.py`、`modules/visualizer.py`、`modules/data_quality.py`、`modules/nwp_forecast.py`、
`modules/reporter.py`、`modules/codec.py`、`config.py`、`modules/aqi.py`、`modules/era5_guide.py`、
`modules/manual.py` 的只读核查，共 167 条带 `file:line` 出处的断言，其中 8 处为跨文件不一致。

本清单是「读图解析与 ERA5 交付」改造的副产品，记录在这里以免丢失。**处置归属**一列写明由哪一批次处理。

| # | 不一致 | 证据 | 影响 | 处置归属 |
|---|---|---|---|---|
| 1 | 主区「使用手册」折叠块写 `GB 3095-2026`（2026-03-01 实施）与 `HJ 633-2026《AQI 技术规定》`，而 AQI 实际口径是 `HJ 633-2012` | `app.py:461`、`app.py:469-470` vs `config.py:534` | 用户在页面上看到的 AQI 标准与实现不符（正是本次要消除的「页面说谎」） | 批次 4：该折叠块整体删除（已被侧边栏手册取代），计划已含此步 |
| 2 | 同处列举「7 个子面板」却把「时间序列」当作面板名 | `app.py:460` vs `visualizer.py:791` | 面板名对不上，用户按名字找不到 | 批次 4：同上，随折叠块删除 |
| 3 | 可视化空气质量 caption 与图标题写 `GB 3095-2026` | `visualizer.py:614`、`visualizer.py:656` | 与 AQI 的 `HJ 633-2012` 口径容易被混为一谈 | 批次 5：改为渲染 `AQI_STANDARD_LABEL` 之外的达标限值口径表述（达标限值与 AQI 分指数是两张表，需分别标注） |
| 4 | Word 报告技术说明指向不存在的常量 | `reporter.py:420` 写「具体阈值参见 config.py 中 WARN_RULES 定义」，`config.py` 无 `WARN_RULES`（实为 `HIGH_TEMP_WARNING` 等 8 个字典） | 报告给出无法执行的指引 | 批次 5：改为指向实际常量集合或删除该句 |
| 5 | 通俗版技术说明与实现不符：穿衣「分 5 级(0-5)」（实现 6 档）、带伞「基于 24h 降水概率分 3 级」（实现依据 72h 累计降水 + 天气码）、紫外线「基于 UV 指数」（实现由 `weather_code` 近似） | `reporter.py:424-428` vs `nwp_forecast.py:1505-1518`、`1520-1528`、`1562-1572` | 报告向用户描述了错误的算法 | 批次 5：按实现改写描述 |
| 6 | 「严重污染」区间上界两版不一致 | `config.py:568` `(301, 500)` vs `nwp_forecast.py:287` `(301, 99999)` | 该差异**不可达**（`iaqi` 最大钳制 500），但表本身应对齐 | 已处理：批次 1 加入 `test_level_tables_do_not_drift`，按截到 500 比对锁死不漂移 |
| 7 | 侧边栏「天气墙封面」按钮夹在调试开关与暗色模式之间，「天气墙」开关在暗色模式之后 | `app.py:624-654` | 两个天气墙控件不相邻，用户不易发现关联 | 批次 5：可选整理（纯可用性，无功能影响） |
| 8 | 两处风玫瑰口径不同：visualizer 版半径=**频次**、9 档、静风单列；nwp 版半径=**频率%**、8 档、静风不出柱 | `visualizer.py:109-119`、`133` vs `nwp_forecast.py:746-753`、`867` | 同一个「风向玫瑰图」在观测页与预报页含义不同（一个读样本数、一个读占比） | 批次 5：在两处图表标题或 caption 中显式标注半径含义；不改算法（属既有设计，非缺陷） |

## 已核对为「一致」的关键点（供后续回归复用）

- AQI 标准标签 `config.AQI_STANDARD_LABEL == "HJ 633-2012"`，六级区间与断点表污染物集合（6 项）与 `modules/aqi.py` 的常量一致。
- ERA5 四个产品名、字段上限（12 000 / 120 000 / 120 000 / 100 000）、八个变量分组、五个预设名与 `modules/era5_guide.py` 定义一致。
- 手册模块的下载按钮标签 `[下载] 用户使用手册 (HTML)`、导出文件名 `用户使用手册.html`、章节选择器标签 `章节导航`、缺文件时的错误文案与 `modules/manual.py` 实现一致。
- 报文解码仅支持 SYNOP 与 METAR 两种，与 `modules/codec.py` 一致；UI 标签为 `SYNOP (地面天气报告)` / `METAR (航空气象报告)`。
- 侧边栏自定义阈值共 7 组，标签与默认值与 `app.py:555-614` 一致。
